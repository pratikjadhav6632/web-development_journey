//que 9.write is the value of each variable in each line of code.

                                      let num=5; 
                                      let newNum=num++; 
                                      console.log(newNum);