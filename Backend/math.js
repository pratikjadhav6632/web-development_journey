export const sum = (a, b) => {
    return a + b;
}

export const sub = (a, b) => {
    return a - b;
}

export const PI = 3.14;
export const g = 9.8;

const obj={
    sum:sum,
    sub:sub,
    PI:PI,
    g:g
};

// module.exports=obj;
